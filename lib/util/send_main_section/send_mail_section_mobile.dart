import 'package:flutter/material.dart';

class SendMailSectionMobile extends StatelessWidget {
  const SendMailSectionMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
